spring:
data:
  mongodb:
      uri: mongodb://ken-milota-1:92631043@localhost:42525/lead-management-system?ssl=true&retryWrites=false

      security:
      oauth2:
        client:
          registration:
            google:
              client-id: ${GOOGLE_CLIENT_ID}
              client-secret: ${GOOGLE_CLIENT_SECRET}
              scope:
                - openid
                - profile
                - email
  application:
    name: User-Service
